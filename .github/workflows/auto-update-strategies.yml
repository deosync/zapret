name: Auto Update Flowseal Strategies

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: auto-update-strategies-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update-strategies:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}  # Используем PAT для checkout

      - name: Add upstream remote & fetch
        run: |
          git remote add upstream https://github.com/Flowseal/zapret-discord-youtube.git || true
          git fetch upstream --depth=1
          
      - name: Debug upstream commits
        run: |
          echo "HEAD of upstream/main:"
          git rev-parse upstream/main
          echo "Saved last commit:"
          if [ -f .last_upstream_commit ]; then cat .last_upstream_commit; else echo "(none)"; fi
          
      - name: Get upstream commit & check
        id: check
        shell: bash
        run: |
          set -e
          UPSTREAM_COMMIT="$(git rev-parse upstream/main)"
          echo "upstream_commit=$UPSTREAM_COMMIT" >> "$GITHUB_OUTPUT"

          if [ -f .last_upstream_commit ]; then
            LAST_COMMIT="$(cat .last_upstream_commit)"
          else
            LAST_COMMIT=""
          fi
          echo "last_commit=$LAST_COMMIT" >> "$GITHUB_OUTPUT"

          if [ "$UPSTREAM_COMMIT" != "$LAST_COMMIT" ]; then
            echo "has_updates=true" >> "$GITHUB_OUTPUT"
            echo "Upstream changed: $UPSTREAM_COMMIT"
          else
            echo "has_updates=false" >> "$GITHUB_OUTPUT"
            echo "No changes in upstream"
          fi

      - name: Download upstream .bat files
        if: steps.check.outputs.has_updates == 'true'
        run: |
          rm -rf temp_upstream upstream_bats || true
          git clone --depth 1 https://github.com/Flowseal/zapret-discord-youtube.git temp_upstream
          mkdir -p upstream_bats
          find temp_upstream -type f -iname "*.bat" -print0 | xargs -0 -I{} cp {} upstream_bats/ || true
          rm -rf temp_upstream
          echo "✅ .bat files gathered"

      - name: Convert .bat -> flowseal-altN.sh
        if: steps.check.outputs.has_updates == 'true'
        run: |
          set -e
          mkdir -p module/strategy
          rm -f module/strategy/flowseal-alt*.sh || true
          python3 scripts/convert_bats_to_strategy.py
          echo "✅ Conversion done. Files placed in module/strategy/"

      - name: Show converted files
        if: steps.check.outputs.has_updates == 'true'
        run: |
          echo "Converted files:"
          ls -1 module/strategy/flowseal-alt*.sh || echo "(no strategy files found)"

      - name: Commit & push changes
        if: steps.check.outputs.has_updates == 'true'
        run: |
          set -e
          git config user.name "github-actions[auto-convert]"
          git config user.email "actions@github.com"
          echo "${{ steps.check.outputs.upstream_commit }}" > .last_upstream_commit
          git add module/strategy/ .last_upstream_commit || true
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          COMMIT_MSG="Auto-convert: Update strategies from upstream ${{ steps.check.outputs.upstream_commit }} [auto-convert]"
          git commit -m "$COMMIT_MSG"
          git push origin HEAD:main --follow-tags

      # Этот шаг вычисляет новый тег
      - name: Calculate new release tag
        if: steps.check.outputs.has_updates == 'true'
        id: calculate_tag
        shell: bash
        run: |
          set -e
          # Получаем последний тег
          LAST_TAG=$(git tag --sort=-v:refname | head -n 1 || echo "")
          if [ -z "$LAST_TAG" ]; then
            echo "No previous tag found. Using 1.0 as base."
            LAST_TAG="1.0"
          fi
          echo "Last tag: $LAST_TAG"

          # Проверяем формат тега (ожидаем X.Y где X и Y - целые числа)
          if [[ ! "$LAST_TAG" =~ ^[0-9]+\.[0-9]+$ ]]; then
            echo "Error: Tag format not supported (expected X.Y, got: $LAST_TAG)"
            exit 1
          fi

          # Разделяем major.minor
          MAJOR=$(echo "$LAST_TAG" | cut -d. -f1)
          MINOR=$(echo "$LAST_TAG" | cut -d. -f2)

          # Увеличиваем минор как целое число
          NEW_MINOR=$((MINOR + 1))
          NEW_TAG="$MAJOR.$NEW_MINOR"
          echo "New tag: $NEW_TAG"
          
          # Сохраняем для следующего шага
          echo "new_tag=$NEW_TAG" >> "$GITHUB_OUTPUT"

      # Создаем тег с помощью PAT, который будет триггерить другие workflow
      - name: Create and push new tag with PAT
        if: steps.check.outputs.has_updates == 'true'
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          set -e
          NEW_TAG="${{ steps.calculate_tag.outputs.new_tag }}"
          
          # Настраиваем git для использования PAT
          git config --global url."https://x-access-token:${PAT_TOKEN}@github.com/".insteadOf "https://github.com/"
          
          # Создаем и пушим тег
          git tag -a "$NEW_TAG" -m "Auto-generated release $NEW_TAG"
          git push origin "$NEW_TAG"
          
          echo "✅ Tag $NEW_TAG created and pushed successfully"

      - name: Add summary to job
        if: steps.check.outputs.has_updates == 'true'
        run: |
          echo "## ✅ Strategies updated" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Upstream commit:** \`${{ steps.check.outputs.upstream_commit }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**New tag created:** \`${{ steps.calculate_tag.outputs.new_tag }}\`" >> $GITHUB_STEP_SUMMARY
          echo "**Status:** Tag pushed successfully and will trigger the build workflow" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Files:**" >> $GITHUB_STEP_SUMMARY
          ls -1 module/strategy/flowseal-alt*.sh | sed -e 's|module/strategy/||g' | while read f; do echo "- $f" >> $GITHUB_STEP_SUMMARY; done
